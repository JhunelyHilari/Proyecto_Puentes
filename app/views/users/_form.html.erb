<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :Nombre %>
  </div>
  <div class="field">
    <%= f.label :Correo %><br>
    <%= f.text_field :Correo %>
  </div>
  <div class="field">
    <%= f.label :Contraseña,"Contraseña"%>
    <%= f.password_field :Contraseña %>
  </div>
  <div class="field">
    <%= f.label :Confirmar_contraseña, "Confirmar Contraseña" %>
    <%= f.password_field :Confirmar_contraseña %>
  </div>
 
     <%= f.submit "Registrar", class: "btn btn-primary" %>

<% end %>
